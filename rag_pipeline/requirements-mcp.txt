# MCP server (mcp_server.py) — exposes the RAG as Model Context Protocol tools.
# Requires Python >= 3.10. Install on top of the core requirements:
#     pip install -r requirements.txt -r requirements-mcp.txt
mcp>=1.2
